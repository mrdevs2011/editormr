# B1–B15 — Reproduce jadvali

> **Muhim cheklov:** bu muhitda brauzer/GUI yo'q — skrinrekord qilib, ilovani
> qo'lda ishlatib reproduce qilib bo'lmadi. Buning o'rniga har bir band **kodni
> o'qib** (grep + qatorma-qator tekshirish) tasdiqlangan yoki rad etilgan.
> "Haqiqiy natija" ustuni shu bilan cheklangan: kod nima qilishi *kerakligini*
> ko'rsatadi, lekin brauzerda haqiqatan shunday ko'rinishini o'zing bir marta
> tekshirib chiqishing kerak (ayniqsa vizual narsalar: B3, B4, B5). Screen
> record talab qilingan joyda buni ochiq belgilab qo'ydim — bu ilova haqiqatan
> ishlab ko'rilmagan.

Ishonch belgilari: ✅ **tasdiqlandi** (kodda aynan shu xatti-harakat bor) ·
❌ **rad etildi / bug emas** (kod boshqacha, roadmap eskirgan) · ❓ **kod bilan
aniqlab bo'lmaydi** (runtime/vizual, brauzer kerak).

| # | Qadamlar | Kutilgan | Haqiqiy (kodga qarab) | Holat |
|---|---|---|---|---|
| **B1** | Timeline'ga matn qo'sh → sahifani yangila (F5) yoki loyihani qayta och. | Matn saqlanib qoladi. | `saveProjectNow` (`projects.js:206,232`) `meta.textClips`ni tayyorlaydi, lekin `dbSaveProject`dagi `row` obyekti (`storage.js:184-198`) uni yozmaydi va `rowToMeta` (`storage.js:72-92`) uni o'qimaydi. DB'da `text_clips` ustuni yo'q. **Matn F5'dan keyin yo'qoladi.** | ✅ tasdiqlandi |
| **B2** | Asosiy qatorga bitta video + bitta rasm clip qo'y (rasm — video oxirida). Export qil, natijani ko'r. | Ikkalasi ham export'da ko'rinadi. | `export.js:333`: `} else if (clip.isImage) { // photo clip — skip }` — asosiy qatordagi rasm clip **butunlay tashlab ketiladi**, faqat `state.isImage===true` bo'lganda (ya'ni hozir preview'da turgan clip rasm bo'lsa) alohida yo'l bilan chiziladi (`export.js:324-332`). Ya'ni ko'p-clipli loyihada oraliq rasm kadrlari qora/bo'sh chiqadi. | ✅ tasdiqlandi |
| **B3** | Gorizontal (16:9) va vertikal (9:16) clip'larni bitta loyihaga qo'y, 9:16 canvas tanla, export qil. | Har clip nisbati buzilmasdan (contain, qora chetlar bilan) chiqadi. | **Roadmap bu yerda eskirgan.** `js/canvas.js` allaqachon mavjud: `getCanvasOutputSize()` tanlangan `state.canvasRatio` presetidan (9:16/1:1/16:9/4:5/...) chiqish o'lchamini hisoblaydi, `canvasDrawContain()` esa har doim **"contain"** (cho'zmasdan, markazlashtirib) chizadi. `export.js`da barcha `drawImage` chaqiruvlari (asosiy clip: qator 361, rasm: 331, float: 393/398) shu funksiya orqali yoki contain-mantiq bilan ishlaydi. `ctx.drawImage(el,0,0,outW,outH)` kabi to'g'ridan-to'g'ri cho'zish **topilmadi**. | ❌ bug emas (eskirgan — kanvas nisbati + fit allaqachon FAZA 2A darajasida qisman qilingan, quyida izoh) |
| **B4** | Matn overlay qo'sh, o'lchamini o'zgartir, preview bilan export'ni solishtir. | Matn o'lchami ikkalasida bir xil ko'rinadi. | Qisman eskirgan: preview `el.style.fontSize = (tc.fontSize * sc)+'px'` (`text-overlay.js:506`), bu yerda `sc = getCanvasPreviewScale()` — preview eni / export eni nisbati (`canvas.js`). Ya'ni **nisbiy o'lcham logikasi qo'shilgan**, faqat vizual tekshiruv (brauzer) buni tasdiqlashi kerak. Lekin shrift oilasi hali ham mos emas: export har doim `bold+fontSize+'px sans-serif'` (`text-overlay.js:640`) yozadi, preview CSS'dagi shrift boshqacha bo'lishi mumkin — buni tekshirmadim (CSS faylni ko'rish kerak). | ❓ qisman (o'lcham mantiqi bor ko'rinadi, shrift oilasi nomuvofiqligi hali tasdiqlanmagan) |
| **B5** | 2 tadan ortiq float (PIP) qator qo'sh, bir vaqtda ko'rinadigan qil. Preview va export'ni solishtir. | Ikkalasida ham hamma float'lar bir vaqtda ko'rinadi, joyi/o'lchami o'zgartirilishi mumkin. | `updatePreviewFloat` (`helpers.js:316`): `floats.length ? floats[floats.length-1] : null` — **faqat oxirgi (eng tepadagi) float** preview'da ko'rsatiladi. `export.js:376-401`: `for (const fc of floats)` — **hammasi** 7% padding bilan `canvasDrawContain` orqali chiziladi (ramka/soya yo'q). Ikkalasi boshqa-boshqa. Butun kodbazada `transform` (x/y/scale/rotation) maydoni **topilmadi** — float joyi/o'lchami sozlanmaydi, tasdiqlangan. | ✅ tasdiqlandi |
| **B6** | HEVC/buzilgan/o'qib bo'lmaydigan video fayl yukla. | Aniq xato xabari, clip yaratilmaydi. | `upload.js:190-201` `getVideoDuration`: `onerror` → `resolve(5)`, va `setTimeout(() => resolve(5), 4000)` — 4 soniyada metadata kelmasa yoki xato bo'lsa, **jim ravishda 5 soniyalik soxta clip** yaratiladi, xatosiz. | ✅ tasdiqlandi |
| **B7** | Export paytida tab'ni fon rejimiga o'tkaz / sahifani yangilashga urin. | Ogohlantirish beriladi (yo'qotish xavfi) yoki ekran o'chmaydi. | `js/export.js` butunlikda `wakeLock` yoki `beforeunload` so'zlari **topilmadi** (butun loyihada ham yo'q). Himoya yo'qligi kodga qarab tasdiqlangan. rAF/captureStream'ning fonda sekinlashishi/buzilishi — bu brauzer xatti-harakati, faqat haqiqiy testda ko'rinadi. | ✅ (himoya yo'qligi) / ❓ (real simptom — brauzer kerak) |
| **B8** | Uzoq (~1 daqiqa+) videoni export qil, ovoz bilan kadrni solishtir. | A/V sinxron ≤40ms. | `export.js:80,316`: drift tuzatish chegarasi `0.12` soniya (120ms) — bu chegaradan katta farq bo'lsagina `currentTime` qayta o'rnatiladi, ya'ni ±120ms gacha drift "sezilmagan" holda qolishi mumkin. Kod mantiqi tasdiqlangan, lekin haqiqiy eshitiladigan drift — brauzer testi kerak. | ✅ (chegara qiymati) / ❓ (haqiqiy eshitiladigan natija) |
| **B9** | Dashboard'ga 2+ faylni bir vaqtda tashla (drag-drop). | Hammasi ketma-ket clip bo'lib qo'shiladi. | `upload.js:359`: `handleFile(e.dataTransfer.files[0])` — faqat **birinchi fayl**, qolganlari e'tiborsiz qoldiriladi. | ✅ tasdiqlandi |
| **B10** | Musiqani bir necha marta almashtir (loadMusicFromFile qayta-qayta chaqiriladi). | Xotira barqaror qoladi. | `music.js:51-52`: har chaqiruvda yangi `new AudioContext()`, `close()` chaqirilmaydi (kodda `close()` umuman yo'q); butun audio fayl `decodeAudioData`bilan to'liq `AudioBuffer`ga dekod qilinadi. Uzun trek + bir necha marta almashtirish = ochiq `AudioContext`lar to'planishi (leak) va katta RAM. | ✅ tasdiqlandi |
| **B11** | — (kod gigiyenasi, funksional emas) | Toza, bitta joyda e'lon qilingan yordamchi funksiyalar. | `upload.js:44` — `trackObjectUrl(trackObjectUrl(...))` ichma-ich ternary (aniq avto-patch izi, real ta'sir yo'q lekin o'qish qiyin). `clipsTimeOverlap` **ikki joyda** e'lon qilingan: `state.js:91` va `drag.js:6` — qaysi biri oxirgi yuklansa, ishlatiladi (script tartibiga bog'liq, fragil). | ✅ tasdiqlandi |
| **B12** | Timeline'da bitta clip qoldir, uni o'chirishga urin. | Bo'sh timeline holati ishlaydi (yoki hech bo'lmasa tushunarli sabab ko'rsatiladi). | `playback.js:87`: oxirgi clip o'chirilganda `showToast('Kamida 1 ta clip qolishi kerak')` va o'chirish bloklanadi — bo'sh timeline holati kod darajasida yo'q. | ✅ tasdiqlandi |
| **B13** | UI matnlarini ko'rib chiq. | Hammasi o'zbekcha. | Ingliz tilida qolgan toast'lar: `music.js:25` `'Music added'`, `music.js:29` `'Failed to load music'`, `upload.js:30` `'Please select a video or image file'` — qolgan qism (masalan `'Clip o'chirildi'`) o'zbekcha. Aralash tasdiqlangan. | ✅ tasdiqlandi |
| **B14** | `/help` sahifasini transition va "qayerda saqlanadi" bo'limlari uchun o'qi. | Hozirgi kod bilan mos. | `help/index.html:183`: **"Preview'da transition ko'rinmaydi — faqat kartalarda va export'da"** — bu noto'g'ri, `updatePreviewTransition` funksiyasi mavjud va preview'da transition ko'rsatadi (`helpers.js` da chaqiriladi). `help/index.html:269`: **"Loyihalar: Supabase + IndexedDB"** — IndexedDB kodda ishlatilmaydi, hamma narsa Supabase'da (`storage.js`). `projects.js:2` izohida ham "IndexedDB" deyilgan (eskirgan izoh, funksional emas). | ✅ tasdiqlandi |
| **B15** | Sahifani ochib, network so'rovlarini ko'r (export bosmasdan). | ffmpeg kutubxonalari faqat export bosilganda yuklanadi. | `index.html:230-231`: `<script src=".../ffmpeg.js">` va `.../util/index.js` — bu ikkala `<script>` tegi **hujjat yuklanishi bilan** (har doim), export bosilishidan qat'i nazar, ishga tushadi. | ✅ tasdiqlandi |

---

## Xulosa: B1-B15 dan nechtasi haqiqiy?

- **✅ To'liq tasdiqlangan (kod darajasida):** B1, B2, B5, B6, B7(himoya yo'qligi), B8(chegara qiymati), B9, B10, B11, B12, B13, B14, B15 — **13 ta**.
- **❌ Bug emas / eskirgan:** **B3** — `js/canvas.js` allaqachon canvas-nisbat presetlari va "contain" (cho'zmaydigan) chizishni amalga oshirgan. Bu roadmap yozilgandan keyin kimdir (yoki avtomatik asistent) qo'shgan bo'lishi mumkin. **Tavsiya: FAZA 2A bo'limini kod bilan qayta solishtir — u yerda "❌ Yo'q funksiyalar" ro'yxatida "Kanvas nisbati (9:16/1:1/4:5)" hali ham yo'q deb yozilgan, lekin bu qisman noto'g'ri: preset tanlash UI + export hisoblash allaqachon bor. Yo'q narsa — foydalanuvchi 9:16/1:1 ni qachon va qayerdan tanlashi (UI oqimi) va pan/zoom, xavfsiz zona kabi qo'shimchalar.**
- **❓ Faqat qisman kod bilan tekshirilgan, brauzer kerak:** B4 (shrift oilasi), B7 (real fon-rejim simptomi), B8 (haqiqiy eshitiladigan drift).

## Keyingi qadam (Faza 0'ning navbatdagi bandi)

Ishonch darajasi yetarli — B1, B6, B9-B12, B14, B15 kabi aniq va kichik tuzatishlarni endi bevosita qo'llash mumkin (roadmap tartibi: B1 → B9 → B10 → B11 → B12 → B13/B14 → B15). B3 esa **tuzatishdan chiqarib tashlanadi** (bug emas). B2, B4, B5, B7, B8 uchun tuzatishdan oldin regressiya fixturalari (Faza 0'ning keyingi bandi) tavsiya qilinadi.
