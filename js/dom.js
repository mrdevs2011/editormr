    // ===================== DOM =====================
    const uploadScreen = document.getElementById('upload-screen');
    const editorScreen = document.getElementById('editor-screen');
    const uploadBox = document.getElementById('upload-box');
    const uploadBtn = document.getElementById('upload-btn');
    const fileInput = document.getElementById('file-input');
    const musicInput = document.getElementById('music-input');
    const previewVideo = document.getElementById('preview-video');
    const previewVideoB = document.getElementById('preview-video-b');
    const previewLayerA = document.getElementById('preview-layer-a');
    const previewLayerB = document.getElementById('preview-layer-b');
    const previewStack = document.getElementById('preview-stack');
    const previewLayerFloat = document.getElementById('preview-layer-float');
    const previewVideoFloat = document.getElementById('preview-video-float');
    const playBtn = document.getElementById('play-btn');
    const timeDisplay = document.getElementById('time-display');
    const musicBtn = document.getElementById('music-btn');
    const exportBtn = document.getElementById('export-btn');
    const timelineScroll = document.getElementById('timeline-scroll');
    const timelineContent = document.getElementById('timeline-content');
    const timelineSection = document.getElementById('timeline-section');
    const timelineResizeHandle = document.getElementById('timeline-resize-handle');
    const timeRuler = document.getElementById('time-ruler');
    const videoLane = document.getElementById('video-lane');
    const musicTrack = document.getElementById('music-track');
    const musicLane = document.getElementById('music-lane');
    const playhead = document.getElementById('playhead');
    const toast = document.getElementById('toast');
    const loading = document.getElementById('loading');
    const blackOverlay = document.getElementById('black-overlay');
    const marqueeEl = document.getElementById('marquee');
    const projectGrid = document.getElementById('project-grid');
    const dashEmpty = document.getElementById('dash-empty');
    const backBtn = document.getElementById('back-btn');

